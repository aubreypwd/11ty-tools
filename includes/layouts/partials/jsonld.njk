{% for key, item in site.jsonld | deepmerge( jsonld ) | default( {} ) %}
	{% if item and item['@type'] and item['@id'] %}
		{% if item['@type'] == 'WebPage' or 'FAQPage' in item['@type'] %}
			{% jsonld item['@type'] | arrayUnique, item['@id'], item, {},
				{
					name: title,
					url: site.baseUrl + page.url,
					description: description
				}
			%}
		{% else %}
			{% jsonld item['@type'] | arrayUnique, item['@id'], item %}
		{% endif %}
	{% endif %}
{% endfor %}
